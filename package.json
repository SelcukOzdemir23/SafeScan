{
  "name": "safescan_flutter_placeholder",
  "version": "0.1.0",
  "description": "Minimal package.json to resolve npm ENOENT error. Flutter dependencies are managed via pubspec.yaml.",
  "private": true,
  "scripts": {
    "info": "echo 'This is a Flutter project. Use pubspec.yaml and flutter pub get for dependencies.'"
  },
  "devDependencies": {},
  "dependencies": {}
}
